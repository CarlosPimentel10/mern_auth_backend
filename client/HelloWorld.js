import React from 'react';
import { hot } from 'react-hot-loader';

const HelloWorld = () => {
    return (
        <div>
            Hello, World.
        </div>
    );
};

export default HelloWorld;
